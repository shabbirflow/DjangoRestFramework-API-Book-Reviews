### DjangoRestFramework-API-Book-Reviews ###

A DRF CRUD API that can connect to ElasticSearch and logs in every HTTP Request ( GET, POST, PUT, etc. ) and sends the logged HTTP requests to ElasticSearch.
